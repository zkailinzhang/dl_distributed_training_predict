---
version: 1

kind: experiment

framework: scikit-learn

tags: [examples]

build:
  image: python:2.7
  build_steps:
    - pip install --no-cache-dir -r requirements.txt

run:
  cmd: python -u model.py
